
<% @signs.each do |sign| %>
	    <div class="col-mod-2">
				     <div class="sign_image"> 
			        	<p><%=link_to image_tag(sign.image_1.url(:thumb)), sign_path></p>
			       	</div>
			    </div>
		
 <% end %>
